# Build output
build/
out/
bin/
dist/
# The per-OS app icons jpackage needs are generated once and committed; the macOS iconset is an intermediate.
!docs/specification/assets/icon/dist/
docs/specification/assets/icon/dist/macos/BookLoom.iconset/

# Gradle
.gradle/
gradle-app.setting
# Kotlin Gradle plugin >= 2.0 writes build-session state here. `build-logic` is the only Kotlin build in the
# repository, but this is left unanchored so a future one is covered too.
.kotlin/
!gradle-wrapper.jar
!gradle-wrapper.properties

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store
Thumbs.db

# Java
*.class
hs_err_pid*
replay_pid*

# Logs / local data
*.log
logs/

# Packaging
*.dmg
*.msi
*.deb
*.rpm
*.pkg
jpackage/

# Context Files
.temporary_context/
.serena/
