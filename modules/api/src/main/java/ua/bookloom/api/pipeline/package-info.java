/**
 * Framework-free translation-engine contracts, job lifecycle types and progress reports.
 *
 * <p>The package is the seam shared by the command line, future JavaFX screens and the pipeline implementation; it
 * contains no document parser, persistence or UI implementation detail.
 */
@NullMarked
package ua.bookloom.api.pipeline;

import org.jspecify.annotations.NullMarked;
