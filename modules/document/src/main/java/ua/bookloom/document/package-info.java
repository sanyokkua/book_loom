/**
 * Document service facade and format dispatch — the implementation of {@code DocumentPort}.
 *
 * <p>Format-specific packages ({@code epub}, {@code fb2}, {@code md}, {@code txt}, {@code mask}, {@code detect},
 * {@code model}) arrive with the round-trip changes.
 */
@NullMarked
package ua.bookloom.document;

import org.jspecify.annotations.NullMarked;
