/**
 * How a failure or a confirmation reaches the person: the blocking error dialog and the transient messages, each a
 * native node inside the shell's own overlays so the token-only theme and the theme swap reach them.
 *
 * <p>Every type here is FX-Application-Thread only. Choosing which surface a given error code goes to is the callers'
 * business, not this package's.
 */
@NullMarked
package ua.bookloom.ui.notify;

import org.jspecify.annotations.NullMarked;
