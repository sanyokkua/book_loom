/**
 * Inference service facade. The only module permitted to import {@code java.net.http} — the structural seed of the
 * offline invariant (DD-01).
 */
@NullMarked
package ua.bookloom.llm;

import org.jspecify.annotations.NullMarked;
